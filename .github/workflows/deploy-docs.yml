name: Deploy Docs

on:
  workflow_dispatch:

# Docs are deployed from nekte-protocol/nekte-protocol.github.io
# which clones this repo and runs its own deploy workflow.
# Trigger manually from: https://github.com/nekte-protocol/nekte-protocol.github.io/actions
# Auto-deploy runs daily at 4am UTC.

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy is managed by nekte-protocol/nekte-protocol.github.io"
